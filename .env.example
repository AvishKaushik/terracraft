# MongoDB connection string (required in production)
# Get yours free at https://mongodb.com/atlas
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/terracraft?retryWrites=true&w=majority

# Server port (deployment platforms set this automatically)
PORT=3001

# Set to "production" so Express serves the built frontend
NODE_ENV=production
